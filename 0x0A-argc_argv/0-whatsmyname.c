#include "main.h"
#include <stdio.h>

/**
 * main - prints it's name followed by a new line
 * @argc: argument count
 * @argv: argument vect0r
 * Return: 0 if success
 */
int main(int argc, char *argv[])
{
	printf("%s\n",argv[0]);
	return (0);
}
