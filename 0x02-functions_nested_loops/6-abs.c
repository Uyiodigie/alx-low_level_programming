#include "main.h"
#include <stdio.h>

/**
 * _abs - computes absolute value of integer
 *
 * @i: integer parameter
 *
 * Return: Always 0 (Success)
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else if (i < 0)
	{
		return (i);
	}
	else
		return (i);
}
